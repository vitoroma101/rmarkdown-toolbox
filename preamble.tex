% Load the hyphenat package to disable hyphenation
\usepackage[none]{hyphenat}
% Load the fancyhdr package and set up the plain page style
\usepackage{fancyhdr}

% Define the initial page style for the document's first pages (e.g., title, table of contents)
\fancypagestyle{plain}{
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0pt}
}
% Apply the plain style as the default for the document
\pagestyle{plain}
% Disable page numbering for the initial pages
\pagenumbering{gobble}

% Define a new pagestyle for the rest of the document
\fancypagestyle{mystyle}{
  \fancyhf{}
  \rfoot{\thepage}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0pt}
}

\usepackage{background}
\backgroundsetup{
  scale=3,
  color=black,
  opacity=0.1,
  angle=45,
  position=current page.center,
  vshift=0pt,
  hshift=0pt,
  contents={\textsf{\Huge vitoroma101}}
}

